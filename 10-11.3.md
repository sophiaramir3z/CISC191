

# Activity 10-11.3: GUI input and formatted text fields

## My Flowchart:
<img width="1024" height="768" alt="Modern Pastel Flowchart (1)" src="https://github.com/user-attachments/assets/1fddb96c-e7fe-4df9-a6cd-0557d905278f" />

## My challenges in performing the lab:
At first, I had trouble figuring out how to extract a value from a JSpinner since it returns an Object, not a numeric type. Casting it properly to double or int took a few tries. 
Aligning components inside the frame was also tricky because FlowLayout didn’t look neat at first, so I switched to GridLayout for even spacing. 
Lastly, I had to ensure that the conversion formula displayed only two decimal places to make the result more readable.
## My code:

```java
import javax.swing.*;
import java.awt.*;
import java.awt.event.*;

public class DistanceConverter {
    public static void main(String[] args) {
        // Create frame
        JFrame frame = new JFrame("Miles to Kilometers Converter");
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        frame.setSize(300, 200);
        frame.setLayout(new GridLayout(3, 1));

        // Spinner for miles input
        JLabel label = new JLabel("Enter distance (miles):");
        SpinnerNumberModel model = new SpinnerNumberModel(0.0, 0.0, 10000.0, 0.1);
        JSpinner spinner = new JSpinner(model);

        // Button and label for result
        JButton convertButton = new JButton("Convert");
        JLabel resultLabel = new JLabel("Result: ", SwingConstants.CENTER);

        // Add functionality
        convertButton.addActionListener(new ActionListener() {
            @Override
            public void actionPerformed(ActionEvent e) {
                double miles = (double) spinner.getValue();
                double km = miles * 1.60934;
                resultLabel.setText(String.format("Result: %.2f km", km));
            }
        });

        // Add components
        frame.add(label);
        frame.add(spinner);
        frame.add(convertButton);
        frame.add(resultLabel);

        // Display
        frame.setVisible(true);
    }
}

```
## Explanation Video:
